# 앱 아이콘 자리 (2.0 마스터링)

여기에 핌 로고 아이콘을 넣는다:
- `icon.ico` (Windows, 256×256 권장)
- `icon.icns` (macOS)
- `icon.png` (512×512, 리눅스/폴백)

넣은 뒤 `electron-builder.yml`·`main.js`의 icon 주석을 해제한다. (없으면 Electron 기본 아이콘 사용)
